import { MediumTitle } from "./";

function GuideCategoryTitle({ text }) {
	const styles = "p-3 pt-1.5 pb-1";
	return <MediumTitle styles={styles} text={text} />;
}

export default GuideCategoryTitle;
